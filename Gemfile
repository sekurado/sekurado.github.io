source "https://rubygems.org"

# Ruby 3.4+ no longer ships these stdlib gems by default.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"

# github-pages pins liquid 4.0.3, which breaks on Ruby 3.2+ (tainted? removed).
gem "liquid", "~> 4.0.4"

gem "github-pages", group: :jekyll_plugins
